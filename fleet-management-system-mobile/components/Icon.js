import React from 'react';
import { default as FontAwesomeIcon } from 'react-native-vector-icons/FontAwesome';

const Icon = ({ name }) => (
    <FontAwesomeIcon name={name} size={15} color="white" />
);

export default Icon;
